{# the header of a page. looks a lot like the block. #}
<header class="{{status}}" style="--img: url({{image}})">
	<span class="top-info">
		<span>
			{% if status %}
			<span class="status">{{status | projectStatus}}</span>
			{% endif %}
			{% if page.date %}
			{% if status %}|{% endif %}
			<span class="dates">
				<time>{{page.date | formatDate}}</time>
					{% if enddate %}
					– <time>{{enddate | formatDate}}</time>
					{% endif %}
			</span>
			{% endif %}
		</span>
		{% if tags.length > 0 %}
		<span class="tags">
			tags: 
			{% for tag in tags %}
			{% if tag !== "post" and tag !== "project"%}
			<span class="tag">{{tag}}</span>
			{% endif %}
			{% endfor %}
		</span>
		{% endif %}
	</span>

	<div>
		<h1><span>{{title}}</span></h1>
		<p>{{description}}</p>
	</div>
</header>